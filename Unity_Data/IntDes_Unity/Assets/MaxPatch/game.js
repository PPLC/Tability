/*
const maxApi = require('max-api');

let x = 0;
let y = 0;


maxApi.addHandler('input', (keyInput) => {
    switch(keyInput){
        case 'UP':
            y--;
            break;
        case 'DOWN':
            y++;
            break;
        case 'LEFT':
            x--;
            break;
        case 'RIGHT':
            x++;
            break;  
    }
    maxApi.post(`x: ${x}, y: ${y}, ${keyInput}`);
    maxApi.outlet(x, y);
});
*/